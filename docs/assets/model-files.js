window.__MODEL_FILES__ = {"file:///dashboards/genre_pairs.malloy":"##! experimental.virtual_source experimental.persistence experimental.join_types experimental.givens experimental.access_modifiers\nimport \"../index.malloy\"\n\n// Two genres and (optionally) one year. GENRE / GENRE2 are the two halves of the\n// combination; the model's `genre` and `genre2` dimensions are independent joins\n// onto the same title, so filtering both finds films carrying BOTH labels.\n//\n// YEAR is a filter<number> whose EMPTY value means \"All Time\" — there is no\n// separate all-time flag, an empty filter simply matches every row. The stepper\n// in genre_pairs.jsx moves it one year at a time.\ngiven:\n  # label=\"Second Genre\" suggest{source=movies dimension=genre}\n  GENRE2 :: filter<string> is f'Romance'\n\n  # label=\"Year\"\n  RELEASE_YEAR :: filter<number> is f''\n\n// Headline numbers for the chosen genre, across all years or just one.\n# artifact { title=\"Genre Combinations\" givens { GENRE=\"Comedy\" RELEASE_YEAR=\"\" } }\n# dashboard\n#\" Pick a genre and see its strongest pairings, shelf by shelf\nquery: genre_pairs is movies -> {\n  where: genre ~ $GENRE and release_year ~ $RELEASE_YEAR\n  aggregate:\n    title_count\n    total_ratings\n    # label=\"Average Rating\"\n    average_rating\n}\n\n// Every genre with how many films carry it — feeds the genre picker. Genres\n// under 100 films are dropped: they make for a long picker and one-tile shelves.\nquery: genre_options is movies -> {\n  group_by: genre is genres.value\n  aggregate: title_count\n  having: title_count >= 100\n  order_by: title_count desc\n}\n\n// One shelf per subgenre: every other genre GENRE pairs with, each nesting its\n// seven most-voted films. Grouping the nest by tconst collapses the\n// principal/genre join fan-out, and total_votes uses max() rather than sum() so\n// it stays the title's own vote count. Shelves are ordered by total_ratings, a\n// symmetric aggregate that survives the fan-out.\nquery: genre_shelves is movies -> {\n  where: genre ~ $GENRE and genre != genre2 and release_year ~ $RELEASE_YEAR\n  group_by: subgenre is genre2\n  aggregate:\n    title_count\n    total_ratings\n  nest: titles is {\n    group_by:\n      tconst\n      title\n      release_year\n      movie_image\n      movie_url\n    aggregate:\n      # label=\"Votes\"\n      total_votes is numVotes.max()\n      average_rating\n    order_by: total_votes desc\n    // Six rows' worth. The shelf shows seven at a time and reveals the rest on\n    // demand, so this is the ceiling on \"Show more\", not what renders up front.\n    limit: 42\n  }\n  order_by: total_ratings desc\n  limit: 24\n}\n\n// The timeline: the chosen genre's films bucketed into 5-year periods, which the\n// bar picker renders and clicks through to a RELEASE_YEAR range. `period` is the\n// first year of the bucket, so 1994 lands in 1990 and the label reads 1990–94.\n//\n// Ignores RELEASE_YEAR on purpose — the whole timeline has to stay visible while\n// one period is selected, otherwise selecting a bar would erase every other bar.\nquery: genre_periods is movies -> {\n  where: genre ~ $GENRE\n  group_by: period is floor(release_year / 5) * 5\n  aggregate:\n    title_count\n    total_ratings\n  order_by: period\n}\n\n// FORK ADDITION (F1): US availability for the titles on this page, so each\n// poster can carry its provider mark. Every row carries JustWatch's\n// attribution because TMDB requires the credit on each media item.\n#\" Where to watch\nquery: availability is providers -> {\n  group_by: imdb_id, offer_kind, provider_name, logo_path, link, attribution\n}\n","file:///dashboards/next_watch.malloy":"##! experimental.virtual_source experimental.persistence experimental.join_types experimental.givens experimental.access_modifiers\nimport \"../index.malloy\"\n\n// Dashboard-local, exactly as genre_pairs.malloy declares its own. GENRE is\n// shared from imdb.malloy; the year range is per-page in upstream's design.\ngiven:\n  # label=\"Year\"\n  RELEASE_YEAR :: filter<number> is f''\n\n// Queries for the recommender. The scoring rationale lives in next_watch.malloy.\n\n#\" What the liked titles have in common, strongest signal first\nquery: taste_profile is taste_features -> {\n  where: tconst ~ $LIKED\n  group_by: kind, feature\n  aggregate: weight is feature_weight\n  order_by: weight desc\n  limit: 60\n}\n\n#\" Recommendations for the current liked set\n# artifact { title=\"Your next watch\" }\nquery: recommendations is scoring -> {\n  // Exclusion of the already-rated happens in the UI, not here: `not (x ~ $G)`\n  // silently returned zero rows for every input, and a filter that quietly\n  // matches nothing is worse than one that is absent.\n  // Kept as a single condition on the join leg. An `OR` across the join field\n  // and the candidate field returned ZERO rows for every input -- Malloy cannot\n  // push a join-side filter through a disjunction, and the failure was silent.\n  // Liked PEOPLE are a separate query (below), merged in the UI.\n  // ⛔ DO NOT ADD `title.genres.value ~ $GENRE` HERE. It was added on 2026-08-04,\n  // compiled, linted green, and was never bundled. `genres.value` is an UNNEST\n  // across a source that is already fanned out one row per (candidate feature x\n  // liked title sharing it): the query went 3.1s -> 60s+ locally and never\n  // returned at all in DuckDB-WASM. Because the dashboard runs its queries in\n  // one queue, that single hang stalled EVERY query on the page -- the genre\n  // chips, the timeline, the poster grid, the swipe deck, all empty, with no\n  // error anywhere. Genre is carried out as a plain list column below and\n  // filtered in the UI, exactly as `director` already is.\n  where:\n    liked_side.tconst ~ $LIKED\n    and title.numVotes ~ $MIN_VOTES\n    and title.startYear ~ $RELEASE_YEAR\n  group_by:\n    tconst\n    primary_title is title.primaryTitle\n    title_type is title.titleType\n    start_year is title.startYear\n    average_rating is title.averageRating\n    num_votes is title.numVotes\n    director is title.directors\n    // scalar list column, NOT an unnest join. Costs nothing and lets the UI do\n    // the genre filter that the where-clause above must never do.\n    genres is title.genres\n    // was missing entirely: the list rendered grey placeholders under a wall of\n    // real posters and read as broken rather than as a different section\n    poster is pick null when poster.poster_path is null\n      else concat('https://image.tmdb.org/t/p/w154', poster.poster_path)\n  aggregate:\n    fit\n    genre_fit\n    crew_fit\n    cast_fit\n    shared_crew\n    shared_cast\n    shared_genres\n    // candidate vector length, for cosine normalisation in the UI\n    norm is title_norm.max()\n  order_by: fit desc\n  limit: 400\n}\n\n// --- the cold start ---------------------------------------------------------\n// Seed titles for the poster grid and the swipe deck. Widely-known enough that\n// a visitor has an opinion, spread across genres so each tap discriminates\n// instead of confirming what everything already shares (CHARTER §4.1).\n#\" Well-known titles to rate, spread across genres\nquery: seed_titles is movies -> {\n  where: numVotes > 150000\n     and genres.value ~ $GENRE\n     and startYear ~ $RELEASE_YEAR\n     and titleType ~ $TITLE_TYPE\n  group_by:\n    tconst\n    primary_title is primaryTitle\n    title_type is titleType\n    start_year is startYear\n    average_rating is averageRating\n    num_votes is numVotes\n    poster is movie_image\n    genre is genres.value\n  order_by: num_votes desc\n  limit: 400\n}\n\n// --- search (F6) ------------------------------------------------------------\n#\" Find a specific title to rate\nquery: search_titles is movies -> {\n  // lowercased both sides: `batman` returned 0 while `Batman` returned 27,\n  // and the failure was silent -- no result, no empty state, no hint why\n  where: lower(primaryTitle) ~ $TITLE\n  group_by:\n    tconst\n    primary_title is primaryTitle\n    title_type is titleType\n    start_year is startYear\n    average_rating is averageRating\n    num_votes is numVotes\n    poster is movie_image\n  order_by: num_votes desc\n  limit: 40\n}\n\n// --- availability (F1) ------------------------------------------------------\n#\" One row per title: the streaming services it is on, for the poster mark.\n// Returning every offer row meant 220,963 rows, which the runtime truncates --\n// so most posters silently lost their badge. Collapsing to one row per title\n// makes coverage complete AND the payload an order of magnitude smaller.\nquery: availability is providers -> {\n  where: offer_kind = 'flatrate'\n  group_by: imdb_id\n  aggregate:\n    logos is string_agg(logo_path, '|')\n    names is string_agg(provider_name, ', ')\n    attribution is max(attribution)\n    votes is title.numVotes.max()\n  // ⛔ THE RUNTIME CAPS A RESULT AT 5,000 ROWS, whatever `limit` says --\n  // measured, not assumed. 15,343 titles have streaming, so a third get\n  // through and WHICH third was arbitrary: the browser was handed obscure\n  // titles while 42 of the 48 most-voted posters on screen lost a badge they\n  // had every right to. Ordering by popularity makes the 5,000 that survive\n  // the ones actually being looked at. A truncated result that still renders\n  // is the worst kind of bug -- nothing fails, it is just quietly wrong.\n  order_by: votes desc\n  limit: 5000\n}\n\n#\" Every viewing option for ONE title, for the detail view\nquery: availability_detail is providers -> {\n  where: imdb_id ~ $DETAIL_ID\n  group_by: imdb_id, offer_kind, provider_name, logo_path, link, attribution\n  order_by: provider_name\n}\n\n// --- people search (F6 / F4) -------------------------------------------------\n// Naming one actor or director is far cheaper for a visitor than naming ten\n// films, so a person is a legitimate way in. Picking one does NOT mark their\n// filmography as liked -- that would recommend someone the worst film of a\n// favourite actor's career and call it a match. It surfaces their titles to\n// rate, which is the honest version of \"seed from a person\" (CHARTER F4).\n#\" Find a person\nquery: search_people is movies -> {\n  where: lower(name) ~ $NAME\n  group_by: person is name\n  aggregate:\n    titles is title_count\n    total_ratings\n  order_by: total_ratings desc\n  limit: 24\n}\n\n#\" Titles featuring the named person, to rate\nquery: titles_by_person is movies -> {\n  where: name ~ $PERSON_EXACT\n  group_by:\n    tconst\n    primary_title is primaryTitle\n    title_type is titleType\n    start_year is startYear\n    average_rating is averageRating\n    num_votes is numVotes\n    poster is movie_image\n  order_by: num_votes desc\n  limit: 40\n}\n\n// --- people as swipe cards (CHARTER F5) --------------------------------------\n// An actor card is a COARSE, high-leverage signal: one swipe touches every title\n// they are in, where a film swipe touches one. So the deck mixes card types and\n// opens with people while the profile is still empty.\n// ⚠ Liking a person raises THEIR weight; it does not mark their filmography as\n// liked. Conflating the two would recommend the worst film of a favourite\n// actor's career and call it a match.\n#\" Well-known people to swipe on\nquery: seed_people is movies -> {\n  // ONLY roles that exist in taste_features (actor/actress -> cast,\n  // director/writer -> crew). The first version grouped over every principal,\n  // so the deck opened on Hans Zimmer -- a composer, who carries no feature at\n  // all, meaning the swipe changed nothing. A card that cannot affect the\n  // result is worse than no card.\n  where: numVotes > 200000\n     and principals.category = 'actor' | 'actress' | 'director' | 'writer'\n  group_by: person is name, nconst is principals.nconst\n  aggregate:\n    titles is title_count\n    total_ratings\n  order_by: total_ratings desc\n  limit: 120\n}\n\n#\" Titles carrying a person the visitor swiped right on\n// Separate from `recommendations` on purpose: see the OR note there. One swipe\n// on an actor touches every title they are in, so this needs no join at all.\nquery: recommendations_by_person is scoring -> {\n  where:\n    // same reason as `recommendations`: no `genres.value` unnest on this source\n    feature ~ $LIKED_PEOPLE\n    and title.numVotes ~ $MIN_VOTES\n    and title.titleType ~ $TITLE_TYPE\n    and title.startYear ~ $RELEASE_YEAR\n  group_by:\n    tconst\n    primary_title is title.primaryTitle\n    title_type is title.titleType\n    start_year is title.startYear\n    average_rating is title.averageRating\n    num_votes is title.numVotes\n    director is title.directors\n    genres is title.genres\n    poster is pick null when poster.poster_path is null\n      else concat('https://image.tmdb.org/t/p/w154', poster.poster_path)\n  aggregate:\n    person_hits is count()\n  order_by: num_votes desc\n  limit: 60\n}\n\n// --- filters, matching what upstream's own pages offer ----------------------\n#\" Genres to filter by\nquery: nw_genre_options is movies -> {\n  group_by: genre is genres.value\n  aggregate: title_count\n  having: title_count >= 100\n  order_by: title_count desc\n}\n\n#\" Titles per five-year period, for the timeline\nquery: nw_periods is movies -> {\n  where: genre ~ $GENRE\n  group_by: period is floor(release_year / 5) * 5\n  aggregate: title_count\n  order_by: period\n}\n\n// A person card with no face is a name in a box, which is what shipped and what\n// Andrew rejected first. The photo comes from person_images (built by\n// scripts/fetch_person_images.py); joining it here rather than in imdb.malloy\n// keeps Lloyd's file untouched.\nsource: movies_with_faces is movies extend {\n  join_one: face is person_images on principals.nconst = face.nconst\n}\n\n#\" People to swipe on, split by whether their work is film or television\nquery: seed_people_typed is movies_with_faces -> {\n  where: numVotes > 200000\n     and (principals.category = 'actor' | 'actress' | 'director' | 'writer')\n     and titleType ~ $TITLE_TYPE\n  group_by:\n    person is name\n    nconst is principals.nconst\n    // w500 because the card is now most of the screen; w342 visibly softens at\n    // that size. Null when TMDB has no portrait -- 1,798 of 13,560 -- rather\n    // than a URL that 404s into a broken-image icon.\n    photo is pick null when face.profile_path is null or face.profile_path = ''\n      else concat('https://image.tmdb.org/t/p/w500', face.profile_path)\n  aggregate: titles is title_count, total_ratings\n  order_by: total_ratings desc\n  limit: 150\n}\n\n// --- the cold start (CHARTER §4) --------------------------------------------\n// \"The tool must produce a defensible list from ZERO explicit input. A blank\n// 'pick 10 movies to begin' screen is a failure of this charter.\"\n// The previous fix DELETED the empty block, which answers Andrew's complaint\n// and breaks the charter in the same edit. This is the list that stands in its\n// place: widely seen and highly rated, inside whatever genre / period / type is\n// selected, labelled for what it is rather than dressed up as personal.\n#\" Acclaimed and widely seen: the list before anyone has rated anything\nquery: popular_picks is movies -> {\n  where: numVotes > 100000\n     and averageRating >= 7.5\n     and genres.value ~ $GENRE\n     and startYear ~ $RELEASE_YEAR\n     and titleType ~ $TITLE_TYPE\n  group_by:\n    tconst\n    primary_title is primaryTitle\n    title_type is titleType\n    start_year is startYear\n    average_rating is averageRating\n    num_votes is numVotes\n    poster is movie_image\n  order_by: average_rating desc\n  limit: 60\n}\n","file:///dashboards/works_together.malloy":"##! experimental.virtual_source experimental.persistence experimental.join_types experimental.givens experimental.access_modifiers\nimport \"../index.malloy\"\n\n// FORK ADDITION (F4). This was previously recorded as a deliberate CUT: this\n// page has no custom React component -- Lloyd renders it with Malloy's built-in\n// table renderer -- and adding provider marks was taken to mean replacing his\n// page with a bespoke component, which §5's style ruling forbids. That premise\n// was wrong. His renderer already draws tagged columns, so availability can be\n// added as one more column IN his table: additive, no component, no redesign.\n// One row per title, so joining it cannot fan the table out.\nsource: title_streaming is providers -> {\n  where: offer_kind = 'flatrate'\n  group_by: imdb_id\n  aggregate: services is string_agg(provider_name, ', ')\n} extend {\n  primary_key: imdb_id\n}\n\nsource: movies_streaming is movies extend {\n  join_one: streaming is title_streaming on tconst = streaming.imdb_id\n  dimension:\n    // TMDB requires the JustWatch credit on EACH media item, so the label\n    // carries it rather than sitting once in a footer.\n    # label=\"Streaming (US, via JustWatch)\"\n    where_to_watch is streaming.services\n}\n\n# artifact { title=\"People that worked together\" givens {NAME='Tom Hanks'}}\nquery: works_together is movies_streaming -> works_with + {\n  where: name ~ $NAME\n  nest: by_title is {\n    group_by: title, where_to_watch\n    aggregate: total_ratings\n  }\n}\n","file:///gs.malloy":"// tables from Google Cloud Storage.\n// Same source names as md.malloy, so switching storage is a one-line import\n// change in imdb.malloy (import \"gs.malloy\" <-> import \"md.malloy\").\n\nsource: people_table is duckdb.table('https://storage.googleapis.com/malloyyo/imdb_names.parquet') extend {}\nsource: principals_table is duckdb.table('https://storage.googleapis.com/malloyyo/imdb_principals.parquet') extend {}\nsource: titles_table is duckdb.table('https://storage.googleapis.com/malloyyo/imdb_titles.parquet') extend {}\n// Upload docs/data/poster_paths.parquet here before switching to this storage.\nsource: poster_paths_table is duckdb.table('https://storage.googleapis.com/malloyyo/poster_paths.parquet') extend {}\n","file:///imdb.malloy":"##! experimental.virtual_source experimental.persistence experimental.join_types experimental.givens experimental.access_modifiers\n\n// storage sources — data/ in this repo, via project-relative paths.\n// gs.malloy (GCS) and md.malloy (MotherDuck) define the same source names.\nimport \"storage.malloy\"\n\ngiven:\n  # label=\"Name\" suggest{query=name_options dimension=name}\n  NAME :: filter<string> is f'Tom Hanks'\n\n  # label=\"Movie Title\"  suggest{source=movies dimension=primaryTitle}\n  TITLE :: filter<string> is f''\n\n  # label=\"Genre\"  suggest{source=movies dimension=genre}\n  GENRE :: filter<string> is f''\n\nsource: people is people_table extend {\n  // cast, crew, everyone involved in movies\n  primary_key: nconst\n}\n\nsource: principals is principals_table extend {\n  // a mapping table that links people to movies, along with their job on that movie.\n  join_one: people is people on nconst = people.nconst\n  measure: principal_count is count()\n}\n\nsource: poster_paths is poster_paths_table extend {\n  // imdb_id -> TMDB poster_path. Rows exist for titles TMDB doesn't have or\n  // has no artwork for, so poster_path is null unless status = 'ok'.\n  primary_key: imdb_id\n}\n\nquery: genre_movie_map is titles_table -> {\n  group_by:\n    tconst\n    genre is genres.value\n}\n\nquery: titles_per_year is titles_table -> {\n  group_by:\n    release_year is startYear\n  aggregate: title_count is count(tconst)\n}\n\n\n#\" IMDB dataset of the the most popular movies in the imdb (by total_votes)\"\n#|(agent) \n   People on a movie are exposed as two flattened pairs so you don't have to\n   walk the principals -> people join:\n     (name,  job)  - a person and the role they played on the movie\n     (name2, job2) - a SECOND, independent person on the same title\n   Group/filter on these directly. Combine the two pairs to relate two people\n   on one movie - e.g. composers who worked with a given director.\n\n   Genres follow the same flattened-pair pattern:\n     genre  - a genre on the movie\n     genre2 - a SECOND, independent genre on the same title\n   Group/filter on these directly. Combine the two to relate two genres on one\n   movie - e.g. how often comedy and romance appear together.\n\n   Possible job / job2 values (most to least common):\n     actor, actress, producer, writer, editor, composer, casting_director,\n     director, cinematographer, production_designer, self, archive_footage,\n     archive_sound\n|#\nsource: movies is titles_table extend {\n  // all the movies\n  join_many: principals on tconst = principals.tconst\n  join_many: principals2 is principals on tconst = principals2.tconst\n  join_many: genre_movie_map on tconst = genre_movie_map.tconst\n  join_many: director is people on directors.each = director.nconst\n  join_one: poster_paths on tconst = poster_paths.imdb_id\n  primary_key: tconst\n\n  measure:\n    # label=\"Title Count\"\n    title_count is count(tconst)\n    row_count is count()\n    # format=\"#,###\" label=\"Total Ratings\"\n    total_ratings is sum(numVotes / 1000.0)\n    # label=\"Average Rating\"\n    average_rating is averageRating.avg()\n    # percent label=\"Percent of Titles\"\n    percent_of_titles is title_count / all(title_count)\n\n  dimension:\n    // TMDB serves images with no auth, so the static site hotlinks the CDN.\n    // poster_path already starts with '/' - don't add another. Null when TMDB\n    // has no artwork, which pick keeps null rather than concat'ing a URL that\n    // would 404. Sizes: w92/w154 table rows, w185 card grids, w342+ detail.\n    # image {width=\"103px\" height=\"154px\"} label=\"Poster\"\n    movie_image is pick null when poster_paths.poster_path is null\n      else concat('https://image.tmdb.org/t/p/w154', poster_paths.poster_path)\n\n    // Same poster at the smallest size TMDB offers, for dense table rows -\n    // a 100-row list at w185 is several megabytes.\n    # image {width=\"46px\" height=\"69px\"} label=\"Poster\"\n    movie_thumb is pick null when poster_paths.poster_path is null\n      else concat('https://image.tmdb.org/t/p/w92', poster_paths.poster_path)\n    # link\n    movie_url is concat('https://www.imdb.com/title/', tconst)\n\n    # label=\"Title\"\n    title is primaryTitle\n\n    # drill {to=[genre_pairs] given=GENRE}\n    genre is genres.value\n    # drill {to=[genre_pairs] given=GENRE2}\n    genre2 is genre_movie_map.genre\n    job is principals.category\n    job2 is principals2.category\n\n    # drill {to=[works_together] given=NAME}\n    name is principals.people.primaryName\n\n    # drill {to=[works_together] given=NAME}\n    name2 is principals2.people.primaryName\n\n    # number=\"####\" label=\"Year\"\n    release_year is startYear\n\n\n  #\" Movies with poster, year and vote count\n  view: by_title is {\n    group_by:\n      tconst, title, release_year, numVotes\n    order_by: numVotes desc\n  }\n\n  # bar_chart\n  #\" Genres by title count and average year\n  view: genre_and_time is {\n    group_by: genre is genre2\n    aggregate: title_count\n    aggregate: avg_year is release_year.avg()\n  }\n\n  # line_chart\n  #\" Titles released per year\n  view: by_year is {\n    group_by: release_year\n    aggregate: title_count\n    order_by: release_year desc\n  }\n\n  #\" People (cast and crew) by ratings and number of titles\n  view: by_name is {\n    group_by: principals.people.primaryName, principals.nconst\n    aggregate: total_ratings, title_count\n  }\n\n  #\" Find people that worked together, filter on name (e.g. where: name = 'Tom Hanks')\n  view: works_with is {\n    where: name != name2\n    group_by: name2\n    aggregate: title_count, percent_of_titles\n    # list\n    nest: by_job is {\n      group_by: job is principals2.category\n      aggregate: title_count\n    }\n    limit: 10\n  }\n\n  #\" Genres by title count and ratings\n  view: by_genre is {\n    group_by: genre is genres.value\n    aggregate: title_count, total_ratings, percent_of_titles\n  }\n\n  #\" Top directors by total ratings\n  view: top_directors is {\n    limit: 100\n    where: job = 'director'\n    group_by: \n      name \n      principals.nconst\n    aggregate: total_ratings\n  }\n\n  #\" Top actors and actresses by total ratings\n  view: top_actors is {\n    limit: 100\n    where: job = 'actor' | 'actress'\n    group_by: name, principals.nconst\n    aggregate: total_ratings\n  }\n}\n\nquery: name_options is movies -> {\n  group_by: name \n  aggregate: total_ratings\n}\n","file:///index.malloy":"import {movies, NAME, TITLE, GENRE, name_options, titles_per_year} from './imdb.malloy'\n// FORK ADDITION: the recommender's sources and controls.\nimport {scoring, taste_features, providers, person_images, LIKED, DISLIKED, TITLE_TYPE, MIN_VOTES, DETAIL_ID, PERSON_EXACT, LIKED_PEOPLE} from './next_watch.malloy'\nexport {movies, NAME, TITLE, GENRE, name_options, titles_per_year}\nexport {scoring, taste_features, providers, person_images, LIKED, DISLIKED, TITLE_TYPE, MIN_VOTES, DETAIL_ID, PERSON_EXACT, LIKED_PEOPLE}\n","file:///md.malloy":"// tables from MotherDuck (mayolo database) — faster than reading parquet from GCS.\n// Same source names as gs.malloy, so switching storage is a one-line import\n// change in imdb.malloy (import \"gs.malloy\" <-> import \"md.malloy\").\n\nsource: people_table is md.table('mayolo.imdb_people') extend {}\nsource: principals_table is md.table('mayolo.imdb_principals') extend {}\nsource: titles_table is md.table('mayolo.imdb_titles') extend {}\n// Load docs/data/poster_paths.parquet into mayolo before switching to this storage.\nsource: poster_paths_table is md.table('mayolo.poster_paths') extend {}\n","file:///next_watch.malloy":"##! experimental.virtual_source experimental.persistence experimental.join_types experimental.givens experimental.access_modifiers\n\n// The recommender. Everything here runs in the visitor's browser over parquet;\n// nothing they tap is transmitted anywhere.\n//\n// SHAPE: liked set -> taste profile -> score every candidate. Two group-bys,\n// because the expensive half (per-feature IDF weights) was precomputed at build\n// time by scripts/build_taste_features.sh.\n//\n// WHY THE SCORING LOOKS LIKE THIS — three attempts, each fixing the last:\n//  1. Raw feature overlap returned ten Coen Brothers films for a liked set of\n//     {Big Lebowski, Fargo, No Country, Pulp Fiction, Snatch}. It had found\n//     \"same director\" and stopped. A filmography is not a recommendation.\n//  2. IDF weighting fixed that, but a single shared jobbing actor still dragged\n//     in Armageddon and Monsters, Inc. -- Steve Buscemi and John Goodman are in\n//     the liked set AND in unrelated blockbusters.\n//  3. Plain cosine was worse: it rewards SHORT feature vectors, so obscure\n//     titles floated to the top.\n// What works, and what is implemented below: genre agreement as a cosine, plus\n// a people term that only counts when at least TWO people are shared. One\n// actor in common is coincidence; a creative team is signal. That list opens\n// Boardwalk Empire, Reservoir Dogs, Hard Eight, Breaking Bad, The Wire.\n\nimport \"storage.malloy\"\n\nsource: taste_features is taste_features_table extend {\n  measure:\n    feature_weight is idf.sum()\n    feature_count is count()\n}\n\ngiven:\n  // The visitor's ratings, as tconst lists. They arrive from the UI, never a\n  // server, and never leave the page.\n  // Sentinel, NOT empty. An empty filter matches EVERY row, which on a\n  // self-join means 167k x 167k -- the engine stalls and the page renders\n  // nothing at all. A value no tconst can equal makes the un-rated case return\n  // zero rows instantly, which is the correct answer to \"recommend me things\n  // based on nothing\".\n  # label=\"Liked\"\n  LIKED :: filter<string> is f'__none__'\n  # label=\"Disliked\"\n  DISLIKED :: filter<string> is f'__none__'\n  # label=\"Type\"\n  TITLE_TYPE :: filter<string> is f''\n  # label=\"Minimum votes\"\n  MIN_VOTES :: filter<number> is f'>= 40000'\n  // which title's full viewing options to fetch; sentinel so the un-opened\n  // state returns nothing rather than every offer for every title\n  # label=\"Detail title\"\n  DETAIL_ID :: filter<string> is f'__none__'\n  // the exact person picked from the results, kept separate from the\n  // lowercased search term\n  # label=\"Person\"\n  PERSON_EXACT :: filter<string> is f'__none__'\n  // people the visitor swiped right on. Their weight rises in the profile;\n  // their filmography is NOT marked as liked.\n  # label=\"Liked people\"\n  LIKED_PEOPLE :: filter<string> is f'__none__'\n  // GENRE and RELEASE_YEAR are UPSTREAM'S, defined in imdb.malloy and already\n  // driving his genre-pairs page. Reused rather than redefined so both pages\n  // filter identically and a fork user is not learning two behaviours.\n\n// Availability, keyed by title. Every row carries JustWatch's attribution\n// because TMDB requires the credit on each media item, not once in a footer.\nsource: person_images is person_images_table extend {\n  primary_key: nconst\n}\n\nsource: providers is watch_providers_table extend {\n  // joined so availability can be ordered by popularity -- see the row cap\n  // documented on the availability query\n  join_one: title is titles_table on imdb_id = title.tconst\n\n  measure:\n    offer_count is count()\n    provider_count is count() { where: offer_kind = 'flatrate' }\n}\n\nsource: scoring is taste_features_table extend {\n  // the same table again, restricted to the visitor's liked titles: a feature\n  // matches when a candidate shares it with something they liked\n  join_many: liked_side is taste_features_table on\n    kind = liked_side.kind and feature = liked_side.feature\n  join_one: title is titles_table on tconst = title.tconst\n  join_one: poster is poster_paths_table on tconst = poster.imdb_id\n\n  dimension:\n    // idf is a property of the FEATURE, so a matched candidate row and its\n    // liked-side partner carry the identical value -- the dot-product term is\n    // idf squared. Written this way because Malloy rejects an aggregate whose\n    // expression spans two join paths, and it needs no cross-join arithmetic.\n    idf_sq is idf * idf\n\n  measure:\n    // dot product of candidate features against the liked profile. The join\n    // fans out one row per (candidate feature x liked title sharing it), so\n    // summing already weights a feature by how many liked titles carry it.\n    fit is idf_sq.sum()\n    // split, because the two behave differently: genre agreement is the broad\n    // shape of a taste and should dominate; shared people are a strong but\n    // narrow signal that must not outvote it. Mixing them into one cosine let\n    // a single prolific actor decide the ranking.\n    genre_fit is idf_sq.sum() { where: kind = 'genre' }\n    crew_fit is idf_sq.sum() { where: kind = 'crew' }\n    cast_fit is idf_sq.sum() { where: kind = 'cast' }\n    // how many DISTINCT people are shared. The threshold this feeds is the\n    // difference between \"Buscemi was also in Armageddon\" and a real overlap.\n    // A shared DIRECTOR or WRITER is a creative-team overlap. A shared ACTOR is\n    // frequently just a jobbing career, which is how Roseanne and Swordfish kept\n    // surfacing for a Coen-brothers profile.\n    shared_crew is count() { where: kind = 'crew' }\n    shared_cast is count() { where: kind = 'cast' }\n    shared_genres is count() { where: kind = 'genre' }\n}\n","file:///storage.malloy":"// Data lives in docs/ — the directory that gets published — referenced by a\n// path relative to the PROJECT ROOT. One spelling works everywhere:\n//\n//   VSCode / malloyyo dashboard dev   DuckDB reads docs/*.parquet off disk\n//   malloyyo dashboard bundle         the site IS docs/, so the page fetches\n//                                     ./imdb_titles.parquet and registers it\n//                                     under this same name\n//\n// Keeping the files in docs/ rather than a separate data/ means they live in\n// git once instead of twice, since docs/ is committed for GitHub Pages.\nsource: people_table is duckdb.table('docs/imdb_names.parquet') extend {}\nsource: principals_table is duckdb.table('docs/imdb_principals.parquet') extend {}\nsource: titles_table is duckdb.table('docs/imdb_titles.parquet') extend {}\n\n// imdb_id -> TMDB poster_path, resolved at build time by\n// scripts/resolve_posters.py. Only the path is stored; the base URL and the\n// size are presentation decisions, applied in imdb.malloy.\nsource: poster_paths_table is duckdb.table('docs/data/poster_paths.parquet') extend {}\n\n// FORK ADDITION: per-(title, feature) IDF weights, built by\n// scripts/build_taste_features.sh. One row per genre or person on a title.\n// This is what the recommender scores against; see next_watch.malloy.\nsource: taste_features_table is duckdb.table('docs/data/taste_features.parquet') extend {}\n\n// FORK ADDITION: US streaming / rent / buy availability, built by\n// scripts/fetch_watch_providers.py from TMDB. Data is JustWatch's and every row\n// carries its attribution, because TMDB requires the credit on each media item.\nsource: watch_providers_table is duckdb.table('docs/data/watch_providers.parquet') extend {}\n\n// FORK ADDITION: nm id -> TMDB profile photo, resolved at build time by\n// scripts/fetch_person_images.py. A person card without a face is just a name\n// in a box.\nsource: person_images_table is duckdb.table('docs/data/person_images.parquet') extend {}\n","file:///transform.malloy":"##! experimental.persistence  experimental.virtual_source\n\nsource: raw_titles is build.sql(\"\"\" \n\t\tSELECT * FROM\n\t\tread_csv_auto(\n\t\t\t-- 'https://datasets.imdbws.com/title.basics.tsv.gz'\n\t\t\t'data/title.basics.tsv.gz'\n\t\t\t, all_varchar=true, delim='\\t', quote='',header=True)\n\t\"\"\")\n\nsource: raw_principals is build.sql(\"\"\" \n\t\tSELECT * FROM\n\t\tread_csv_auto(\n\t\t\t-- 'https://datasets.imdbws.com/title.principals.tsv.gz'\n\t\t\t'data/title.principals.tsv.gz'\n\t\t\t, all_varchar=true, delim='\\t', quote='',header=True)\n\t\"\"\")\n\nsource: raw_ratings is build.sql(\"\"\" \n\t\tSELECT * FROM\n\t\tread_csv_auto(\n\t\t\t-- 'https://datasets.imdbws.com/title.ratings.tsv.gz'\n\t\t\t'data/title.ratings.tsv.gz'\n\t\t\t, all_varchar=true, delim='\\t', quote='',header=True)\n\t\"\"\")\n\nsource: raw_crew is build.sql(\"\"\" \n\t\tSELECT * FROM\n\t\tread_csv_auto(\n\t\t\t-- 'https://datasets.imdbws.com/title.crew.tsv.gz'\n\t\t\t'data/title.crew.tsv.gz'\n\t\t\t, all_varchar=true, delim='\\t', quote='',header=True)\n\t\"\"\")\nsource: raw_names is build.sql(\"\"\" \n\t\tSELECT * FROM\n\t\tread_csv_auto(\n\t\t\t-- 'https://datasets.imdbws.com/name.basics.tsv.gz'\n\t\t\t'data/name.basics.tsv.gz'\n\t\t\t, all_varchar=true, delim='\\t', quote='',header=True)\n\t\"\"\")\n\ntype: title_type is {\n\ttconst:: string,\n\tisAdult:: string,\n\toriginalTitle:: string,\n\tprimaryTitle:: string,\n\tstartYear:: number,\n\tendYear:: number,\n\truntimeMinutes:: number,\n\tgenres:: string[],\n\tdirectors:: string[],\n\twriters:: string[],\n\taverageRating:: number,\n\tnumVotes:: number,\t\n\toverall_rank:: number,\n\tyear_rank:: number\n}\n\n#@ persist name=\"titles\"\nsource: titles_base is raw_titles -> {\n\tjoin_one: raw_ratings on tconst = raw_ratings.tconst\n\tjoin_one: raw_crew on tconst = raw_crew.tconst\n\t// FORK CHANGE: television included alongside film.\n\t// Upstream was `titleType = 'movie'`. A watchlist people actually keep is\n\t// mixed, so excluding TV was excluding half the answer.\n\t//   tvSeries / tvMiniSeries - the shows themselves\n\t//   tvMovie                 - one-off films made for TV; a film by any\n\t//                             reasonable reading, and people search for them\n\t// Deliberately NOT tvEpisode: an episode is not a thing you choose to watch\n\t// tonight, and including them would swamp the corpus with hundreds of\n\t// thousands of rows that no recommendation should ever return.\n\twhere:\n\t\traw_ratings.numVotes::number > 5000\n\t\tand titleType = 'movie' | 'tvSeries' | 'tvMiniSeries' | 'tvMovie'\n\tselect:\n\t\ttconst\n\t\t// Carried so the UI can say \"series\" vs \"film\", rank them separately,\n\t\t// and hit the right TMDB endpoint. Votes are NOT comparable across\n\t\t// these: a long-running series accumulates votes a film cannot.\n\t\ttitleType\n\t\tisAdult, originalTitle, primaryTitle\n\t\tstartYear is startYear:::number \n\t\tendYear is endYear:::number\n\t\truntimeMinutes is runtimeMinutes:::number\n\t\tgenres is str_split!string[](genres,',')\n\t\tdirectors is str_split!string[](raw_crew.directors,',')\n\t\twriters is str_split!string[](raw_crew.writers,',')\n\t\taverageRating is raw_ratings.averageRating:::number\n\t\tnumVotes is raw_ratings.numVotes:::number\n\tcalculate:\n\t\toverall_rank is rank() {order_by: averageRating desc}\n\t\tyear_rank is rank() {partition_by: startYear order_by: averageRating desc}\n}::title_type extend {\n\tview: test is {\n\t\tselect: *\n\t\tlimit: 10\n\t}\n}\n\ntype: principal_type is {\n\ttconst:: string,\n\tordering:: number,\n\tnconst:: string,\n\tcategory:: string,\n\tjob:: string,\n\tcharacters:: string[]\n}\n\n#@ persist name=\"principals\"\nsource: principals_base is raw_principals -> {\n\tjoin_one: titles is titles_base on tconst = titles.tconst\n\twhere: titles.tconst is not null\n\tselect: \n\t\ttconst\n\t\tordering is ordering::number\n\t\tnconst\n\t\tcategory\n\t\tjob\n\t\tcharacters is str_split!string[](\n\tregexp_replace!(characters,'[\\[\\]\\\"]','','g'),\n\t',')\n}::principal_type extend {\n\tview: test is {\n\t\tselect: *\n\t\tlimit: 10\n\t}\n}\n\ntype: year_ratings is {\n  startYear:: number,\n  year_ratings_acting:: number,\n  year_ratings_directing:: number,\n  year_rank:: number\n}\n\ntype: person_type is {\n  nconst:: string,\n  birthYear:: number,\n  deathYear:: number,\n  primaryName:: string,\n  primaryProfession:: string,\n  lifetime_rank_acting:: number,\n  lifetime_ratings_acting:: number,\n  lifetime_rank_directing:: number,\n  lifetime_ratings_directing:: number,\n  by_year:: year_ratings[]\n}\n\n#@ persist name=\"names\"\nsource: people_base is   raw_names -> {\n\tjoin_many: principals is principals_base on nconst = principals.nconst\n\tjoin_one: titles is titles_base on principals.tconst = titles.tconst\n\twhere: principals.nconst is not null\n\tgroup_by:\n\t\tnconst\n\t\tprimaryName\n\t\tprimaryProfession\n\t\tbirthYear is birthYear:::number\n\t\tdeathYear is deathYear:::number\n\t\ttitles.startYear\n\taggregate:\n\t\tyear_ratings_acting is titles.numVotes.sum() {where: principals.category = 'actor' | 'actress' }\n\t\tyear_ratings_directing is titles.numVotes.sum() {where: principals.category = 'director' }\n\tcalculate:\n\t\tyear_rank is rank() {partition_by: startYear order_by: year_ratings_acting desc}\n\t}\n\t-> {\n\t\tgroup_by:\n\t\t\tnconst\n\t\t\tprimaryName\n\t\t\tprimaryProfession\n\t\t\tbirthYear is birthYear:::number\n\t\t\tdeathYear is deathYear:::number\n\t\taggregate:\n\t\t  lifetime_ratings_acting is year_ratings_acting.sum()\n\t\t  lifetime_ratings_directing is year_ratings_directing.sum()\n\t\tcalculate:\n\t\t  lifetime_rank_acting is rank() {order_by: lifetime_ratings_acting desc}\n\t\t  lifetime_rank_directing is rank() {order_by: lifetime_ratings_directing desc}\n\t\tnest: by_year is {\n\t\t\tgroup_by:\n\t\t\t\tstartYear\n\t\t\t\tyear_ratings_acting\n\t\t\t\tyear_ratings_directing\n\t\t\t\tyear_rank\n\t\t}\n\t\torder_by: nconst\n\t}::person_type\n\n"};
window.__TABLE_FILES__ = {"docs/data/person_images.parquet":"./data/person_images.parquet","docs/data/poster_paths.parquet":"./data/poster_paths.parquet","docs/data/taste_features.parquet":"./data/taste_features.parquet","docs/data/watch_providers.parquet":"./data/watch_providers.parquet","docs/imdb_names.parquet":"./imdb_names.parquet","docs/imdb_principals.parquet":"./imdb_principals.parquet","docs/imdb_titles.parquet":"./imdb_titles.parquet"};
window.__DUCKDB_BASE__ = "./duckdb/";
